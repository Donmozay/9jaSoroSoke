@model IEnumerable<_9jasorosoke.Repositories.Models.CarOwner>

@{
    var ProofOfVehicleOwnerShip = Model.FirstOrDefault().ProofOfVehicleOwnerShip;
    var PurchaseReciept = Model.FirstOrDefault().PurchaseReciept;
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProofOfVehicleOwnerShip)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PurchaseReciept)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <img src="@ProofOfVehicleOwnerShip" id="idimg" style="width: 50px; height: 50px;" />

                </td>
                <td>
                    <img src="@PurchaseReciept" id="idimg" style="width: 50px; height: 50px;" />
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                </td>
            </tr>
        }
    </tbody>
</table>
